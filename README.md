# shopEase
 **ShopEase** is a full-stack e-commerce app built with React (Vite + Tailwind) and Spring Boot. It uses JWT authentication, a RESTful API, and MySQL with JPA. Users can browse, search, manage a cart, and place orders, with React Context handling global state.
